extends blog-base


block blog-content
    .row
        .col-8.offset-2
            br
            a(href='#')
                h2 #{post.title}
            em #{post.published.toDateString()} #{post.published.toLocaleTimeString('en-US', {hour12: false})}
            | !{post.content}
