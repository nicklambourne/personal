extends blog-base


block blog-content
    .row
        .col-10.offset-1
            br
            a(href='#')
                h2 #{post.title}
            em #{post.published.toDateString()} #{post.published.toLocaleTimeString('en-US', {hour12: false})}
            | !{post.content}
